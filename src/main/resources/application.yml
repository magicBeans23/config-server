server:
  port: 8888

spring:
  application:
    name: config-server
  #profiles:
    #active: native
  cloud:
    config:
      server:
        git:
          uri: https://github.com/magicBeans23/config-server.git
          cloneOnStart: true

management:
  security:
    enabled: false