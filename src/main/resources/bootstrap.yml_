server:
  port: 8888

spring:
  profiles:
    active: native
  cloud:
    config:
      server:
        git:
          uri: /Users/shivam/Workspace/config-server
          cloneOnStart: true

management:
  security:
    enabled: false